<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="vpiTabPageIndicatorStyle">@style/CustomTabPageIndicator</item>
    </style>

    <style name="MMTabButton">
        <item name="android:textSize">12.0dip</item>
        <item name="android:gravity">center_horizontal</item>
        <!--<item name="android:background">@drawable/bg_checkbox_menus</item>-->
        <item name="android:layout_width">fill_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:button">@null</item>
        <item name="android:textColor">@color/tab_font_orange_color</item>
        <item name="android:layout_weight">1.0</item>
        <item name="android:paddingBottom">2.0dip</item>
        <item name="android:paddingTop">2.0dip</item>
    </style>


    <style name="author_photo_style" parent="@android:style/Widget.ImageWell">
        <item name="android:layout_width">60dp</item>
        <item name="android:layout_height">60dp</item>
        <item name="android:layout_gravity">center_vertical</item>
        <item name="android:layout_marginLeft">10dp</item>
        <item name="android:layout_marginRight">10dp</item>
        <item name="android:background">@drawable/author_photo_style</item>
    </style>

    <!-- tab的 style -->
    <style name="CustomTabPageIndicator">
        <item name="android:gravity">center</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:background">@color/white</item>

        <item name="android:paddingTop">4dp</item>
        <item name="android:paddingLeft">8dp</item>
        <item name="android:paddingBottom">4dp</item>
        <item name="android:paddingRight">8dp</item>

        <item name="android:textAppearance">@style/CustomTabPageIndicator_text</item>
        <!--<item name="android:textColor">@color/selector_tab</item>-->
        <item name="android:textSize">18sp</item>

        <!-- 分割线，可有可无的 -->
        <item name="android:divider">@mipmap/vote_black</item>
        <item name="android:dividerPadding">10dip</item>
        <item name="android:showDividers">middle</item>
        <item name="android:fadingEdge">horizontal</item>
        <item name="android:fadingEdgeLength">8dp</item>
    </style>
    <style name="CustomTabPageIndicator_text" parent="android:TextAppearance.Medium">
        <item name="android:typeface">monospace</item>
    </style>
</resources>
